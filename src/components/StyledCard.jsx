function StyledCard() {
  const styles = {
    color: "#0e314c",
    backgroundColor: "#a3cceb",
    padding: "20px",
    borderRadius: "10px",
  };

  return (
    <div style={styles}>
      <h2>Code-enx</h2>
      <p>The only identity</p>
    </div>
  );
}

export default StyledCard;
